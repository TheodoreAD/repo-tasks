name: publish

# Publish repo-tasks to a package index with the repo's own `inv dist.publish` —
# plans/2026-08-22-pypi-publish-integration.md §4. TestPyPI is unconditional and runs first; the
# real index is a second job behind the `pypi` GitHub Environment, whose required-reviewer rule is
# the actual safety gate: a version uploaded to pypi.org can never be re-uploaded, even after
# deletion, so a human confirms before that step, every time.
#
# Auth is Trusted Publishing (OIDC): `permissions: id-token: write` lets uv exchange the job's
# short-lived GitHub ID token for a short-lived upload token, so no PyPI secret is stored anywhere.
# Both indexes need a one-time "pending publisher" registered by hand for
# TheodoreAD/repo-tasks + this filename (and, for pypi.org, the `pypi` environment) before the
# first run can succeed — that setup lives outside this repo by design.
#
# Release candidates (`vX.Y.ZrcN`, tagged on the release branch by `inv gitflow.release-candidate`)
# reach TestPyPI only: the real-index job is skipped for any tag carrying `rc`, so a candidate is
# never one approval click away from pypi.org. A final tag runs both.
# DISABLED 2026-09-04, deliberately and declaratively: the tag trigger is removed, leaving only
# manual dispatch, so nothing here fires on its own. Two reasons, and the second is why this is not
# merely tidy-up.
#
# The blocking one: the "pending publisher" registration this file's Trusted Publishing depends on
# has never been done on either index, so every run would fail at upload — see
# plans/2026-08-22-pypi-publish-integration.md, still open on exactly that.
#
# The surprising one: with the tag trigger live, `inv trunkflow.cut` did not mean "bump the
# version", it meant "upload to TestPyPI and queue a PyPI approval", because the TestPyPI job here
# is unconditional. Pushing a tag is the release gate across this ecosystem — requests, flask and
# httpx all publish on a tag push, and PyPA's own guide says to push a tagged commit to publish —
# so a task that pushed a tag as a side effect of bumping was one command away from publishing.
# `trunkflow.cut` no longer pushes by default; this removes the other half of that coupling.
#
# Re-enabling is this block plus the registration, and nothing else. Kept as a live workflow file
# rather than renamed to `.disabled` so actionlint and zizmor keep checking it — a disabled file
# that stops being linted is one that rots before anyone tries to switch it back on.
#
# One consequence to know before dispatching this by hand, pre-existing but now the only path: the
# `pypi` job's `if: !contains(github.ref_name, 'rc')` reads a *tag* name under the trigger that was
# just removed, and reads a *branch* name under dispatch. Dispatched from `main` it is therefore
# always true, and the job would try to publish whatever version `pyproject.toml` currently holds
# rather than a tagged release. The `pypi` environment's reviewer rule is what still stands between
# that and pypi.org.
on:
  workflow_dispatch:

# Workflow-level grant is the floor every job inherits, so `id-token: write` is declared per job
# rather than here — a job added later for something unrelated (a changelog step, a notification)
# would otherwise silently inherit the one credential in this repo worth stealing.
permissions:
  contents: read

# Actions are pinned to full-length commit SHAs here, and only here. A tag is a mutable ref: whoever
# can move `v4` can run their code in a job holding `id-token: write` against PyPI Trusted
# Publishing, which is the one credential in this repo worth stealing. ci.yml keeps plain tags —
# pinning everywhere without dependabot means pins that silently rot, and dependabot means a standing
# PR stream on a repo that is pushed to directly. One file's pins are maintainable by hand.
#
# To refresh: `gh api repos/<owner>/<repo>/git/ref/tags/<tag> --jq .object.sha`, and update the
# trailing comment to match.
jobs:
  testpypi:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      # enable-cache: false, not the `auto` default. `auto` disables the cache for a tag push but
      # not for the workflow_dispatch path this file also carries, and a cache restored into a job
      # holding id-token: write is a write-up primitive: any lower-privilege workflow on the repo
      # can seed the cache this one then unpacks. A once-per-release job gains nothing from it.
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: false
      - run: ./bootstrap.sh
      - run: inv dist.publish --index testpypi

  pypi:
    needs: testpypi
    if: ${{ !contains(github.ref_name, 'rc') }}
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: false
      - run: ./bootstrap.sh
      - run: inv dist.publish
