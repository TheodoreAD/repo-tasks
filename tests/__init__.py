"""Test suite for repo-tasks.

A package, not a bare directory: pytest recommends packaging tests under the `prepend` import mode
this family uses, and it is what lifts the globally-unique-basename requirement an unpackaged tree
imposes. See contributing/type-checking.md.
"""
