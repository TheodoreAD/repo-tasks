# Non-python artifacts this repo ships. Python projects are not listed here — `uv`'s own
# [tool.uv.workspace] in pyproject.toml is their source of truth (see contributing/ and
# src/repo_tasks/projects.py).
#
# The one entry pair below is this repo's dogfood sample: a docker image and the helm chart that
# wraps it, sharing `group = "sample-service"` with the workspace member of the same name, so one
# `inv version.bump --group sample-service` moves all three together.

[[docker]]
name = "sample-service"
# The repo root, not examples/sample-service — a uv workspace member's dependencies resolve from
# the workspace root's pyproject.toml and uv.lock, which a narrower context would not contain.
path = "."
dockerfile = "examples/sample-service/Dockerfile"
# Lowercase owner segment, deliberately: GHCR rejects any uppercase character in an image ref, and
# this account's GitHub username is mixed-case (TheodoreAD). Confirmed by the registry smoke test
# in plans/2026-08-22-docker-registry-integration.md.
image = "ghcr.io/theodoread/sample-service"
group = "sample-service"
